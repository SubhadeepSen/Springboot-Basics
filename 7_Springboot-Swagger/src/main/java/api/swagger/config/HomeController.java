/*package api.swagger.config;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

This controller is just to display the Swagger home page on calling the URL: localhost:8080
One can do the same without this controller just by calling the URL: localhost:8080/swagger-ui.html

@Controller
public class HomeController {
    @RequestMapping(value = "/")
    public String index() {
        return "redirect:swagger-ui.html";
    }
}
*/